# Claim Boundary Note

Date: 2026-07-03

Open-MaoField is a public sanitized release surface, not a mirror of the private
MaoField canonical research repository.

## Safe Current Claim

The current mathematical object is a finite positive weighted two-way table
`X = Q x B` with weighted Hilbert space `L^2(w)`, constant subspace `C`,
centered row-main-effect subspace `A`, centered column-main-effect subspace
`B_0`, and additive nuisance space `N_add = C + A + B_0`.

The current bounded result is:

- product weights are equivalent to `A` being orthogonal to `B_0`;
- product weights are equivalent to order-independence of the two declared
  sequential stripping maps;
- non-product weights give an existential pure-main-effect witness, not a
  universal statement about every input;
- the nonzero wrong-order sequential output is a procedural artifact, not a
  true additive residual;
- the exact 2x2 rational witness is the certificate-level example.

## Forbidden Upgrades

Do not upgrade this repository into claims about:

- any positive empirical finding from MaoField;
- model-fitting runs, checkpoint-based inference or a newly authorized
  optimization objective;
- full-panel execution;
- F3 or LOSO success;
- deployed-model residual-field measurements;
- solved black-box interpretability;
- a general new ANOVA theory;
- a general new dependent-input decomposition theory;
- a broad new projection-product theory;
- JSON floats or deterministic harness output proving the theorem.

## Harness Boundary

The canonical sentence is:

```text
The floating-point harness is deterministic regression support only; the mathematical claims are carried by the analytic proof and exact rational certificate, not by JSON floats.
```
