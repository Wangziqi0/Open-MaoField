# Paper Source

`preprint.tex` is the sanitized public draft source for:

```text
Finite Projection Order Defects in Residual Metric Audits:
A Mathematical Starting Point for Black-Box Evaluation
```

The source uses inline `thebibliography` entries. A compiled PDF is not included
in this seed because the node36 export environment did not have a LaTeX
toolchain.
